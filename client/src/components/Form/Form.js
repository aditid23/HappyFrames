import React from 'react';
// import { StyledPaper, StyledForm, FileInput, SubmitButton } from "./styles";

const Form = () =>{
    return(
        <h1>FORM</h1>
    );
}

export default Form;